class ChooseLevel : ZilchComponent
{
    function Initialize(init : CogInitializer)
    {
        Zero.Connect(Zero.Keyboard, Events.KeyUp, this.OnKeyDown);
    }
  function OnKeyDown(event : KeyboardEvent)
    {
    }
}
